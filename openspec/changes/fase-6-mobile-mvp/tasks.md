## 1. Setup Expo

- [x] 1.1 `npx create-expo-app apps/mobile --template blank-typescript`, integrar ao workspace pnpm/Turborepo (scaffold manual, sem CLI interativo, seguindo convenções do monorepo — ver commit)
- [x] 1.2 Adicionar deps: `expo-router`, `expo-sqlite`, `@supabase/supabase-js`, `react-native-reanimated`, `react-native-gesture-handler`, `@forja/motor-narrativo`, `@forja/dominio`, `@forja/schema` (workspace:*)
- [x] 1.3 Confirmar `pnpm --filter @forja/mobile lint`/`typecheck` rodam (mesmo sem código de app ainda)
- [x] 1.4 Code review (paralelo): revisar setup/config antes de prosseguir para storage

## 2. Storage local (SQLite) — mobile-offline-diary

- [x] 2.1 `src/storage/schema.ts`: tipo de linha local (superset de `DiaryEvent`: `id_local`, `campaign_instance_id`, `tipo`, `payload`, `device_id`, `idempotency_key`, `app_version`, `ocorrido_em`, `synced_at`, `server_id`)
- [x] 2.2 `src/storage/sqlite.ts`: `initDB`, `insertEvent`, `getAllEvents`, `getUnsyncedEvents`, `markSynced(ids, serverIds)`
- [x] 2.3 `src/storage/toDominioEvent.ts`: mapear linha local → `DiaryEvent` de `@forja/dominio`
- [x] 2.4 Unit tests: insert/query/mark-synced, mapeamento para `DiaryEvent`
- [x] 2.5 Code review (paralelo): revisar storage antes de prosseguir para sync — achou `insertEvent` não idempotente (teste mascarava); corrigido para `INSERT OR IGNORE` + constraint composta `(campaign_instance_id, idempotency_key)`, tiebreaker de ordenação adicionado

## 3. Sincronização via Supabase SDK — mobile-offline-diary

- [x] 3.1 `src/lib/supabase.ts`: client Supabase configurado (URL/anon key via env), sessão via `supabase.auth`, `AsyncStorage` como storage adapter (RN não tem `window.localStorage`)
- [x] 3.2 `src/sync/SyncService.ts`: `push()` (insert em chunks dos eventos com `synced_at` nulo, marca `synced_at` só após confirmação), `pull()` (`select` de eventos novos por `campaign_instance_id`)
- [x] 3.3 Tratamento de idempotência: reenvio após falha não deve duplicar (constraint única do servidor); rejeição por duplicata busca o `server_id` real (não grava null — evitava stall do cursor de `pull()`)
- [x] 3.4 Unit tests: push/pull com mock do client Supabase (sucesso, falha parcial, rede indisponível, cursor de pull com server_id null/fora de ordem)
- [x] 3.5 **Integration test**: repo não tem stack Supabase local provisionada (sem CLI, sem `supabase/config.toml`) — mesmo precedente de `apps/api/test/e2e/*.e2e-spec.ts`, que também stuba o client em vez de Postgres/PostgREST real. Teste de integração cobre `SyncService` real × `storage/sqlite.ts` real cruzando a fronteira entre os dois (nem um nem outro mockado entre si), contra um stub de Supabase — não verifica RLS real do servidor (gap registrado em design.md)
- [x] 3.6 Code review (paralelo): revisar SyncService — checar aderência a ADR-0011 (sem endpoint custom) antes de prosseguir. Confirmado: sem `/sync` em `apps/api`. Achados corrigidos: bug de cursor stall em `pull()` (dedupe gravava `server_id: null`), sessão Supabase sem storage adapter em RN, mutation tests ausentes (adicionado `stryker.config.mjs`, score 60.77% ≥ threshold 60)

## 4. Hooks de integração motor/domínio — mobile-session-flow

- [x] 4.1 `src/hooks/useFicha.ts`: `calcularFicha(eventos)` sobre eventos locais mapeados, memoizado por mudança do log
- [x] 4.2 `src/hooks/useResolution.ts`: monta `Inputs` a partir da ficha corrente, chama `resolve()` do motor, grava evento `sessao_registrada`. `State.qualities` reconstruído em `src/narrative/state.ts` a partir do snapshot mais recente de `sessao_registrada` (não fold por diff — `newState` do motor já é cumulativo, ver `resolve.ts`). Simplificação MVP registrada em comentário: `ciclo_cumprido`/`tregua`/`reencontro`/`sessao_secundaria` default `false` (derivação real exige dados fora de `Ficha`, DEC-011/DEC-033)
- [x] 4.3 `src/hooks/useJuramento.ts`: `validarJuramento`, grava evento `juramento_declarado` se válido
- [x] 4.4 `content/`: **gap real** — não existe catálogo de storylets autoral em `content/` neste monorepo (só fixtures de teste). Copiado `packages/motor-narrativo/tests/fixtures/minimal-catalog.json` para `src/content/catalog.json` como placeholder explícito (comentado em `catalog.ts`), import estático (D-036, sem fetch). Substituir quando o catálogo real existir
- [x] 4.5 Unit tests: `useFicha`, `useJuramento`, `useResolution`, `narrative/state` — mock de storage/expo-crypto/expo-constants, harness `renderHook` próprio (`src/testUtils/renderHook.ts`, `react-test-renderer`, sem `@testing-library/react-hooks` descontinuado)
- [x] 4.6 **Integration test** (`hooks/session-flow.integration.spec.ts`): fluxo Juramento → Sessão → Resolução ponta a ponta contra storage SQLite real + `@forja/dominio`/`@forja/motor-narrativo` reais (só bindings nativos sem equivalente em Node mockados: `expo-sqlite`/`expo-crypto`/`expo-constants`/AsyncStorage), validando ficha recalculada corretamente após cada evento
- [x] 4.7 Code review (paralelo): GO, sem bloqueios. D-033 confirmado (grep: nenhuma referência a apps/mobile/react/react-native dentro de packages/motor-narrativo|dominio); reconstrução de `narrative/state.ts` verificada correta contra `resolve.ts`/`recognition.ts`/`safety-net.ts`/`selector/bag.ts` (newState sempre snapshot cumulativo completo, nunca diff); placeholder de catálogo e simplificações de MVP confirmadas como devidamente sinalizadas em comentário, não escondidas. Nitpicks não bloqueantes: path do catálogo placeholder difere do previsto em design.md D6 (ok, catálogo real ainda não existe); seed `Date.now()` em `resolve()` pode colidir em duplo-tap no mesmo milissegundo — considerar debounce quando o botão "Registrar Sessão" existir (Grupo 5)
- Mutation tests: glob do `stryker.config.mjs` estendido para `src/narrative/**` e `src/hooks/**`; score total 65.05% (≥ break 60), reforçado após pedido do review (era 64.08% no primeiro run)

## 5. Telas MVP + navegação — mobile-app-shell

- [x] 5.1 `app/_layout.tsx` + navegação Expo Router (file-based) entre as 7 rotas. `CampaignProvider` (`src/context/CampaignContext.tsx`) instancia `useFicha`/`useResolution` uma única vez para todo o app (`initDB()` + `getOrCreateActiveCampaignInstanceId()` no boot) — telas consomem via `useCampaign()` em vez de cada uma criar sua própria instância dos hooks, evitando dessincronia entre elas (mesmo D-033/D5: hooks continuam sendo a única porta de motor/domínio, isto só compartilha essa porta)
- [x] 5.2 `HomeScreen` (`app/index.tsx`): dashboard via `useFicha` (atributos, vontade, fôlego, ciclo, Juramento) + links de navegação
- [x] 5.3 `JuramentoScreen` (`app/juramento.tsx`): form (seleção 1-6 dias) via `useJuramento`, exibe erro de validação RF-004
- [x] 5.4 `SessaoScreen` (`app/sessao.tsx`): botão "Registrar Sessão" via `useResolution`. Nitpick do review do Grupo 4 (seed `Date.now()` pode colidir em duplo-tap) endereçado: botão desabilitado enquanto a chamada está em andamento
- [x] 5.5 `ResolucaoScreen` (`app/resolucao.tsx`): exibe texto/efeitos do `ultimoResultado` (estado do hook compartilhado via `CampaignProvider`); estado vazio (nenhuma sessão ainda nesta instância do app) tratado
- [x] 5.6 `HistoricoScreen` (`app/historico.tsx`): lista eventos do log local (`ficha.eventos`) mais recentes primeiro
- [x] 5.7 `CompartilharScreen` (`app/compartilhar.tsx`): **simplificação MVP** — compartilha resumo em texto da ficha via `Share` nativo, não screenshot (exigiria `react-native-view-shot`, dep. não instalada, fora do orçamento das 7 telas)
- [x] 5.8 `ConfigScreen` (`app/config.tsx`): sync manual (`SyncService` real contra `supabase`), exportação LGPD (`src/api/lgpd.ts`, `GET /data-export` com JWT de sessão), logout. Inclui também login por magic link (`supabase.auth.signInWithOtp`) — resolve o Open Question de design.md (autenticação como parte desta tela, não uma 8ª tela dedicada); sem ela `auth.uid()` nunca resolve e sync nunca funciona. `src/api/lgpd.ts` é implementação mínima com unit tests próprios; testes de integração contra `apps/api` real ficam no Grupo 6 (6.3/6.4), que também adiciona `src/api/entitlements.ts`
- [x] 5.9 Code review (paralelo): **GO**. D-033/D-036/ADR-0011 confirmados sem violação (grep independente); hooks React chamados corretamente (nenhum uso condicional/dentro de callback); simplificações de MVP sinalizadas em comentário e tasks.md, não escondidas. Nitpicks corrigidos: `SessaoScreen` usava `useState<Error>` próprio duplicando `resolution.error` do hook (removido, agora usa só a fonte única); `ConfigScreen` não diferenciava status de sucesso/erro visualmente (agora `{mensagem, erro}` com estilo vermelho em falha). Achado importante não bloqueante, registrado como débito abaixo: atribuição de `user_id` em eventos offline
- **Débito registrado (não bloqueante, revisitar antes do Grupo 6 fechar auth)**: `campaignInstanceId` é gerado por device (`src/lib/campaignInstance.ts`), independente de sessão Supabase; `SyncService.push()` grava `user_id` de quem estiver logado *no momento do sync* em todo evento pendente (`SyncService.ts:73`). Em device compartilhado, eventos gravados offline antes de qualquer login ficam atribuídos a quem primeiro sincronizar, sem rebind/aviso ao trocar de usuário — relevante para LGPD (mistura de dados entre identidades). MVP assume 1 usuário por device (D-001), então não bloqueia, mas precisa de decisão explícita quando auth for endurecida
- Gap registrado: criação real da linha `campaign_instances` no servidor (D4 de design.md) não implementada — `src/lib/campaignInstance.ts` gera/persiste só um id local estável (offline-first, D-008); linha remota só passará a existir quando o Grupo 6/sync tratar disso, ou como débito futuro se não for bloqueante
- Mutation tests: glob do `stryker.config.mjs` estendido para `src/lib/campaignInstance.ts` e `src/api/**` (demais arquivos de `src/lib/` — `ids.ts`/`dados.ts`/`supabase.ts` — excluídos por serem wrappers finos de binding nativo sem lógica própria, mesmo racional da exclusão de telas/JSX); score total 66.52% (≥ break 60)
- Unit tests: 30 passando (`useJuramento`/`useFicha`/`useResolution`/`narrative/state`/`sync`/`storage` já existentes + novos `lib/campaignInstance.spec.ts`, `api/lgpd.spec.ts`); integration tests: 5 passando (inalterados do Grupo 4, telas não têm harness de integração própria — Non-Goal de design.md, sem Detox nesta fase)

## 6. Integração apps/api — mobile-app-shell

- [x] 6.1 `src/api/entitlements.ts`: `validarEntitlement(platform, receipt)` chama `POST /entitlements/validate` autenticado (JWT Supabase via `getAccessToken()`), tipos `EntitlementPlatform`/`EntitlementValidateResponse` de `@forja/schema` (fonte de verdade compartilhada com `apps/api`, contrato conferido contra `entitlements.controller.ts`/`packages/schema/src/entitlements.ts`)
- [x] 6.2 `src/api/lgpd.ts`: já implementado no Grupo 5 (`exportarDadosLGPD()`, `GET /data-export`) — sem mudanças nesta tarefa
- [x] 6.3 Unit tests: `entitlements.spec.ts` (4 testes: sem sessão não chama fetch, POST com corpo/headers corretos, resposta não-ok lança erro, erro de rede propaga). `lgpd.spec.ts` já existia do Grupo 5 (3 testes)
- [x] 6.4 **Integration test** (`src/api/apps-api.integration.spec.ts`): **correção do enunciado** — `docker-compose.yml` existe na raiz, mas só provisiona credenciais Supabase fictícias (`SUPABASE_URL: 'http://localhost:54321'`, service-role key fake), então validação real de JWT/RLS não é exercitável hoje mesmo com ele de pé (achado do code review; mesma conclusão do gap original, premissa corrigida). Servidor `node:http` puro reimplementa o contrato real: valida request contra `EntitlementValidateRequestSchema` real de `@forja/schema`, responses no formato conferido contra `apps/api/test/e2e/{entitlements,lgpd}.e2e-spec.ts` reais. Cruza a fronteira de rede de verdade (fetch real, parsing real, client `entitlements.ts`/`lgpd.ts` reais, não mockados) — só o servidor é substituído. Não valida AuthGuard/RLS/DB reais — gap registrado, mesmo racional de 3.5. 3 testes: validate bem-sucedido, 401 sem Authorization, data-export bem-sucedido
- [x] 6.5 Code review (paralelo): **GO**. Contrato request/response conferido exato contra `packages/schema`/`apps/api` reais; auth consistente com ADR-0011 (sem reimplementar JWT, mesmo padrão de `lgpd.ts`); sem imports de `@forja/motor-narrativo`/`@forja/dominio` (D-033, grep limpo); sem leak de token/receipt em mensagens de erro; mutation score 68% real. Nitpick corrigido acima (docker-compose existe mas com credenciais fictícias)

## 7. Verificação final

- [x] 7.1 `pnpm --filter @forja/mobile lint` — limpo
- [x] 7.2 `pnpm --filter @forja/mobile typecheck` — limpo
- [x] 7.3 `pnpm --filter @forja/mobile test` (unit) — 34/34 passando (10 arquivos)
- [x] 7.4 `pnpm --filter @forja/mobile test:integration` — 8/8 passando (3 arquivos: SyncService, session-flow, apps-api)
- [ ] 7.5 Rodar app em simulador (iOS ou Android, o que estiver disponível no ambiente) — **gap declarado**: ambiente sem `xcrun`/`emulator`/`adb` disponível (confirmado por busca nos binários), sem simulador para rodar. Fluxo completo manual (Juramento → Sessão → Resolução → Ficha, offline/online) fica pendente de execução em ambiente com simulador real. `hooks/session-flow.integration.spec.ts` cobre o fluxo ponta a ponta contra storage real, mas **não** é substituto equivalente para a fiação real de tela (achado do review de 7.7: esse teste compõe seus próprios hooks à mão, nunca passa por `CampaignContext`/`useCampaign()` como as telas de fato fazem — foi exatamente por isso que o bug de `SessaoScreen` não aparecia nele). `CampaignContext.integration.spec.ts` (adicionado em 7.7) cobre melhor a fiação real, mas ainda sem UI/navegação — só simulador real ou Detox (fora de escopo, design.md Non-Goals) fecham essa lacuna de fato
- [x] 7.6 Grep de imports em `@forja/motor-narrativo`/`@forja/dominio` a partir de `apps/mobile`: `grep -rl` em `app/` (as 7 telas) retorna zero matches — confirmado que só `src/hooks/` e módulos de suporte interno (`src/narrative`, `src/storage`, `src/content/catalog.ts`, `src/lib/dados.ts`) importam os pacotes, nunca as telas diretamente (D-033)
- [x] 7.7 Code review final (paralelo): **NO-GO na primeira passada → GO após correção**. Achado bloqueante real: `useResolution` mantém instância própria e interna de `useFicha` (só p/ recalcular `vontade` entre rolagens), inteiramente separada do `ficha` de nível superior exposto por `CampaignContext` a Home/Histórico/Compartilhar. `SessaoScreen` só chamava `resolution.registrarSessao()` (refrescava a instância interna) sem nunca chamar `ficha.refresh()` do contexto — essas 3 telas ficavam com dados desatualizados após qualquer sessão registrada (mesma dessincronia que `CampaignProvider` foi criado para evitar, reintroduzida um nível abaixo). `hooks/session-flow.integration.spec.ts` não pegava isso por compor seus próprios hooks à mão, nunca passando por `CampaignContext`.
  - **Corrigido**: `app/sessao.tsx` agora chama `await ficha.refresh()` (do `useCampaign()`) logo após `resolution.registrarSessao()`, mesmo padrão já usado em `app/juramento.tsx`.
  - **Teste de regressão adicionado**: `src/context/CampaignContext.integration.spec.ts` — 2 testes cruzando `CampaignProvider` real × hooks reais × storage real (só bindings nativos mockados, mesmo precedente de 4.6). Um confirma que `ficha` de nível superior reflete a sessão quando `refresh()` é chamado (fluxo real pós-fix); outro documenta que, sem esse `refresh()`, a dessincronia reaparece (prova que o teste pegaria a regressão se reintroduzida). Exigiu `esbuild.jsx: 'automatic'` em `vitest.integration.config.ts` (primeiro teste a importar um `.tsx` diretamente; não afeta o transform real do Metro/Expo em produção)
  - Suítes completas re-rodadas após o fix: lint/typecheck limpos, 34 unit + 10 integration passando (era 8, +2 do teste de regressão)
  - **Nitpicks não bloqueantes registrados**: (a) `src/api/entitlements.ts`/`validarEntitlement` está implementado e testado (6.1/6.3) mas não é chamado por nenhuma das 7 telas — não há UI de compra/IAP no MVP; `spec.md` descreve um cenário de usuário completando compra que ainda não está de fato encadeado no app (mismatch spec/implementação, não bloqueante — escopo de 6.1 já era "wrapper de cliente", não a UI). (b) Débito do gap D4 (sem linha `campaign_instances` real no servidor) estava subestimado: `SyncService.push()` insere eventos referenciando um `campaign_instance_id` sem linha correspondente — um schema real com FK em `campaign_instances` rejeitaria todo push hoje, não é só "gap a revisitar depois"
